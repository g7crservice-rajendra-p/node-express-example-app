# =============================================================================
# Environment config template — tokens replaced at deploy time from Variable Groups
# Token format: __VAR_NAME__ (e.g. __APP_PORT__, __PGSQL_SERVER__)
# =============================================================================

NODE_ENV=production
APP_PORT=__APP_PORT__
